<?xml version="1.0" encoding="UTF-8" ?>
<mvc:View
    controllerName="pp.ofupload.controller.Main"
    xmlns:mvc="sap.ui.core.mvc"
    displayBlock="true"
    xmlns="sap.m"
    xmlns:unified="sap.ui.unified"
    xmlns:upload="sap.m.upload"
    xmlns:table="sap.ui.table"
>
    <Page>
        <content>
            <Panel
                expandable="true"
                expanded="false"
                headerText="{i18n>templateText}"
                width="auto"
            >
                <upload:UploadSet
                    id="idTemplateCollection"
                    items="{ path : 'Attach>/Configuration' }"
                    multiple="true"
                    uploadEnabled="false"
                    uploadButtonInvisible="true"
                    instantUpload="false"
                >
                    <upload:items>
                        <upload:UploadSetItem
                            fileName="{Attach>TemplateName}"
                            mediaType="{Attach>TemplateMimeType}"
                            url="{Attach>TemplateContent}"
                            visibleEdit="false"
                            visibleRemove="false"
                        />
                    </upload:items>
                </upload:UploadSet>
            </Panel>
            <Panel
                id="idFileUploadPanel"
                expandable="true"
                expanded="true"
                headerText="{i18n>uploadText}"
                width="auto"
            >
                <unified:FileUploader
                    id="idFileUploader"
                    sameFilenameAllowed="true"
                    fileType="xlsx,xls"
                    change="onFileUploaderChange"
                    width="40%"
                    class="sapUiSmallMargin"
                    enabled="{= ${local>/authorityCheck/button/Upload}}"
                />
            </Panel>
            <Panel
                expandable="true"
                expanded="true"
                headerText="{i18n>result}"
                width="auto"
            >
                <table:Table
                    id="uploadTable"
                    rows="{local>/excelSet}"
                    selectionMode="Multi"
                    rowMode="Interactive"
                    class="sapUiSizeCompact"
                >
                    <table:extension>
                        <OverflowToolbar>
                            <Text text="{local>/logInfo}" />
                            <ToolbarSpacer />
                            <Button
                                id="idCheckButton"
                                icon="sap-icon://check-availability"
                                type="Transparent"
                                text="{i18n>checkBtn}"
                                press="onCheck"
                                visible="{= ${local>/excelSet}.length > 0}"
                                enabled="{= ${local>/authorityCheck/button/Check}}"
                            />
                            <Button
                                id="idExcuteButton"
                                icon="sap-icon://document"
                                type="Transparent"
                                text="{i18n>excuteBtn}"
                                press="onExcute"
                                visible="{= ${local>/excelSet}.length > 0}"
                                enabled="{= ${local>/authorityCheck/button/Execute} &amp;&amp; ${local>/recordCheckSuccessed}}"
                            />
                            <Button
                                id="idExportButton"
                                icon="sap-icon://excel-attachment"
                                type="Transparent"
                                text="{i18n>exportBtn}"
                                press="onExport"
                                visible="{= ${local>/excelSet}.length > 0}"
                                enabled="{= ${local>/authorityCheck/button/Export}}"
                            />
                        </OverflowToolbar>
                    </table:extension>
                    <table:columns>
                        <table:Column
                            autoResizable="true"
                            width="6rem"
                            label="{i18n>Status}"
                        >
                            <table:template>
                                <ObjectStatus
                                    state="{ path:'local>Type',formatter:'.formatter.setState' }"
                                    icon="{ path:'local>Type', formatter:'.formatter.setStateIcon'}"
                                />
                            </table:template>
                        </table:Column>
                        <table:Column
                            width="16rem"
                            label="{i18n>Message}"
                        >
                            <table:template>
                                <Text
                                    text="{local>Message}"
                                    tooltip="{local>Message}"
                                    wrapping="false"
                                />
                            </table:template>
                        </table:Column>
                        <table:Column
                            width="8rem"
                            label="{i18n>Customer}"
                        >
                            <table:template>
                                <Text
                                    text="{local>Customer}"
                                    wrapping="false"
                                />
                            </table:template>
                        </table:Column>
                        <table:Column
                            width="6rem"
                            label="{i18n>Version}"
                        >
                            <table:template>
                                <Text
                                    text="{local>Version}"
                                    wrapping="false"
                                />
                            </table:template>
                        </table:Column>
                        <table:Column
                            width="10rem"
                            label="{i18n>Material}"
                        >
                            <table:template>
                                <Text
                                    text="{local>Material}"
                                    wrapping="false"
                                />
                            </table:template>
                        </table:Column>
                        <table:Column
                            width="6rem"
                            label="{i18n>Plant}"
                        >
                            <table:template>
                                <Text
                                    text="{local>Plant}"
                                    wrapping="false"
                                />
                            </table:template>
                        </table:Column>
                        <table:Column
                            width="8rem"
                            label="{i18n>RequirementDate}"
                        >
                            <table:template>
                                <Text
                                    text="{local>RequirementDate}"
                                    wrapping="false"
                                />
                            </table:template>
                        </table:Column>
                        <table:Column
                            width="8rem"
                            label="{i18n>RequirementQty}"
                        >
                            <table:template>
                                <Text
                                    text="{local>RequirementQty}"
                                    wrapping="false"
                                />
                            </table:template>
                        </table:Column>
                        <table:Column
                            width="8rem"
                            label="{i18n>DeliveryQty}"
                        >
                            <table:template>
                                <Text
                                    text="{local>DeliveryQty}"
                                    wrapping="false"
                                />
                            </table:template>
                        </table:Column>
                        <table:Column
                            width="8rem"
                            label="{i18n>UndeliveryQty}"
                        >
                            <table:template>
                                <Text
                                    text="{local>UndeliveryQty}"
                                    wrapping="false"
                                />
                            </table:template>
                        </table:Column>
                        <table:Column
                            width="12rem"
                            label="{i18n>Remark}"
                        >
                            <table:template>
                                <Text
                                    text="{local>Remark}"
                                    tooltip="{local>Remark}"
                                    wrapping="false"
                                />
                            </table:template>
                        </table:Column>
                    </table:columns>
                </table:Table>
            </Panel>
        </content>
    </Page>
</mvc:View>
